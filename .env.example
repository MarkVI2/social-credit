# MongoDB Configuration
# Replace with your actual MongoDB connection string
MONGODB_URI=mongodb://localhost:27017/social-credit

# For MongoDB Atlas (cloud), use this format:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/social-credit?retryWrites=true&w=majority

# Application Configuration
NODE_ENV=development

# Public App URL (used in emails for links)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# SMTP (Email) Configuration
# Configure these to enable email sending (verification, reset, change notifications)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password
# The From header for outgoing emails
MAIL_FROM="Social Credit" <no-reply@example.com>

# Gmail option (no domain needed):
# - Turn on 2-Step Verification and create an App Password, then fill below.
# - If SMTP_HOST is omitted, the app will default to smtp.gmail.com when SMTP_USER ends with @gmail.com
# SMTP_USER=yourgmail@gmail.com
# SMTP_PASS=<your 16-char app password>
# MAIL_FROM=Your Name <yourgmail@gmail.com>
