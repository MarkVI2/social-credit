// import { recordActivity } from "./logService";
import { TransactionLog } from "./transactionService";

export async function recordTransactionActivity(t: TransactionLog) {
  // await recordActivity({
  //   type: "transaction",
  //   action: "credit_transfer",
  //   data: t,
  //   undone: false,
  // });
}
